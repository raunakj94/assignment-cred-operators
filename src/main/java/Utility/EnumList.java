package Utility;

public class EnumList {

    public enum Gender {Male,Female}
    public enum AccountType {ACTIVE,BLOCK,WITHHOLD,CLOSED}
    public enum PreferredModeOfPayment {COD, Wallet, CreditCard, DebitCard, UPI}
    public enum CustomerType {PRIME, AD_PRIME, NORMAL}
}
